# Create vector
pencils <- c(25, 23, 12, 11, 6, 7, 8, 9, 10)

# Mean
mean(pencils)

# Median
median(pencils)

# Mode (custom function)
getmode <- function(x) {
  ux <- unique(x)
  ux[which.max(tabulate(match(x, ux)))]
}

getmode(pencils)
